## Agent skills

### Issue tracker

Issues live as GitHub issues on `monktana/pokemon-quiz`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context layout: `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
